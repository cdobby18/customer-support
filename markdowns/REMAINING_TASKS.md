# Remaining Tasks - Ordered by Implementation Priority

| Order | Task | Agent | Reason |
|-------|------|-------|--------|
| 1 | ~~Add LLM Gateway & Configuration~~ **DONE** | Foundation | `app/llm.py`: provider-agnostic gateway (`LLM_PROVIDER` env: mock/openai/azure_openai; opt-in; production rejects mock), centralized chat/generate/generate-json entry points, process-local rate limiting (`LLM_RATE_LIMIT_PER_MINUTE`), retry-with-backoff (`LLM_MAX_RETRIES`), token + estimated-cost tracking with admin `GET /admin/llm/usage` + reset endpoint, prompt/response redacted logging. `app/prompts.py`: named/versioned prompt template registry (triage.classify, response.draft, escalation.summary, agent_assist.suggest) with strict render checks. Real providers call Chat Completions over raw HTTPS with injected transport (no SDK dep). |
| 2 | ~~Implement Semantic Search (Embeddings + Vector DB)~~ **DONE** | Knowledge Agent | `app/embeddings.py` upgraded to provider-aware embeddings (`EMBEDDING_PROVIDER` env): `local` (sentence-transformers, default, no key) or `openai` (`EMBEDDING_MODEL`, default `text-embedding-3-small`, requires `OPENAI_API_KEY`, batched via `EMBEDDING_BATCH_SIZE`). FAISS inner-product index retained as the vector store; the `data/embeddings` cache stores a provider+model signature so switching providers/models auto-rebuilds (e.g. 384→1536 dim). L2 normalization + dimension-mismatch guard. Config validated by `validate_security_configuration` (production OpenAI embeddings require a key). |
| 3 | ~~Build Response Agent with Guardrails~~ **DONE** | Response Agent | `app/response_agent.py`: `draft_reply()` grounds drafts in KB via `search_knowledge`, calls the LLM gateway with `response_format="json_object"` and the versioned `response.draft_json` template, matches "Source N" citations, derives confidence (model or best retrieval score), flags for human review on no KB matches / empty draft / confidence below `RESPONSE_CONFIDENCE_THRESHOLD` (default 0.75) / model escalation / guardrail violations. `POST /tickets/{ticket_id}/response-draft` (staff-only; 503 config / 502 provider errors; `response.draft_generated` audit). **Self-service automation added:** `POST /tickets/{ticket_id}/auto-respond` + opt-in intake hook (`AUTO_RESPOND_ENABLED`) — safe drafts are sent to the customer as a "Relay AI" comment and the ticket resolved (deflection); everything else escalates to humans (`pending` + `requires_human_review` + `response.auto_escalated`). Frontend: AI Draft panel (staff) with Generate + Auto-respond + confidence/citations/guardrails, and "Relay AI" chat labels for customers. Provides the `DraftResult`/`AutoRespondResponse` contract Task 9's AI-draft review UI will consume. |
| 4 | **Upgrade Triage to LLM-Based Classification** | Triage Agent | Keyword rules don't generalize. LLM with structured output (JSON schema) handles nuance, multi-intent, and unseen categories. |
| ~~Add Intake Normalization Layer~~ **DONE** | Intake Agent | Channel adapters for email/chat/Slack/WhatsApp/CRM in `app/intake.py`; `intake_metadata` JSON column; customer context enrichment. |
| 6 | **Build Escalation Intelligence** | Escalation Agent | Beyond keyword triggers: detect risk via sentiment + intent + customer tier + SLA breach. Auto-route to correct team with context summary. |
| 7 | **Implement Agent Assist Features** | Agent Assist Agent | Summarize ticket history, find similar resolved cases, suggest reply templates, surface relevant KB articles in real-time. |
| 8 | ~~Add Evaluation & Feedback Loop~~ **DONE** | Evaluation Agent | Collect CSAT (POST /tickets/{id}/feedback + new feedback table), deflection/resolution tracking, admin analytics endpoint |
| 9 | ~~Build Human Review Dashboard (Frontend)~~ **DONE (non-LLM parts)** | Frontend | Review/approve escalations, internal notes, SLA visibility (badge + overdue highlight + sort/filter) built. Remaining: approve/reject AI drafts w/ KB citations (blocked on Tasks 3/7). |
| 10 | ~~Add Async Job Queue (Celery + Redis)~~ **DONE** | Orchestration | Celery app (app/workers.py) w/ Redis broker + eager-by-default dev mode (no Redis needed). `support.notify` task posts to NOTIFY_WEBHOOK_URL w/ 3 retries; wired into /tickets (ticket.created) + webhooks (ticket.received). Production: set CELERY_TASK_ALWAYS_EAGER=0, run `celery -A app.workers:celery_app worker`. |
| 11 | ~~Implement Guardrail/Policy Engine~~ **DONE** | Guardrails | Deterministic engine (app/guardrails.py): PII regex (email/phone/SSN/credit-card Luhn/IPv4), compliance+legal keyword rules, policy blocklist + high-value refunds, response validation hooks. Wired into intake (tickets + webhooks): flag + audit + auto-escalation on high risk. |
| 12 | ~~Add Multi-Channel Orchestration~~ **DONE** | Intake/Workflow | Threading via thread_id (replies join open tickets as comments; pending→open; resolved threads create a new ticket), external_id dedup (idempotent retries, 200 + existing ticket), channel-aware SLA (SLA_CHANNEL_MULTIPLIERS env, chat/WhatsApp faster than email). Migration `0010_add_channel_threading_fields`. |
| 13 | ~~Build Analytics Dashboard~~ **DONE** | Evaluation/Frontend | Aggregated `GET /admin/analytics/dashboard` (SLA breakdown, CSAT, escalation rate, team workload, channels/priorities/intents/sentiments breakdowns, triage confidence histogram). Admin "Analytics" nav view (AnalyticsView) with stat cards + bar breakdowns, no chart lib. |
| 14 | ~~Add CRM/Helpdesk Integrations~~ **DONE (layer + mock)** | Integrations | Provider-agnostic outbound layer `app/integrations.py`: provider registry driven by `SUPPORT_TOOL_PROVIDER` (mock/zendesk/hubspot), opt-in (unset = disabled). Outbound create/update + comment push wired into create/webhook/update/comment; sync state recorded in `intake_metadata.integration` + `integration.synced` audit logs; internal (private) comments not pushed; comment push skips when no remote ticket id. Zendesk/HubSpot adapters gated behind API creds (raise IntegrationError until configured). Real vendor endpoints tested via injected fake transports. |
| 15 | ~~Production Hardening~~ **DONE** | All | Observability (OpenTelemetry tracing + structured JSON logs + X-Request-Id correlation), load testing (Locust suite + committed baseline), config hardening (SUPPORT_TOOL_PROVIDER validation, audit retention + admin purge endpoint). Secrets management, disaster recovery, audit compliance, and chaos engineering documented as runbooks in `markdowns/runbooks/`. |
| 16 | **Full Codebase Review & Simplification** | All | Final cleanup pass: audit `app/`, `tests/`, `frontend/` for dead code, unused imports/arguments, duplicated logic, over-engineering, and redundant migrations/docs. Prefer less over more — remove anything not needed, simplify for readability and explainability. Verify with the full test suite (156 tests) after pruning; aim to shrink LOC without changing behavior. Also verify no secret values, API keys, tokens, or private/internal functions are exposed anywhere in the codebase (repo + docs + reports) before finalizing. Keep `user@gmail.com` dev account. |